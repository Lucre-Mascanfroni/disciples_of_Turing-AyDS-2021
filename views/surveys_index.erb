<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <title> SURVEY </title>
  </head>

  <body>
    <h1> UNRC SURVEY </h1>
    <form id = 'form' action='/responses' method='POST'> <!-- Inicio del formulario -->
    
        <ul>
            <% @questions.map do |question| %> <!-- Por cada pregunta -->
              <li> <%= question.number %> <%= question.description %> </li> <!-- La mostramos -->
              <ul>
                  <% question.choices.map do |choice| %> <!-- Por cada opcion asociada a la pregunta -->
                      <input type='radio' name='<%= question.id%>' value='<%= choice.id%>'> <%= choice.text%> </input> 
                      <!-- Por cada opcion generamos un 'boton' para poder seleccionarla, las opciones son excluyentes -->
                  <% end %>
                  <input type='hidden' name='question_id[]' value='<%=question.id%>'>
              </ul>
            <% end %>
        </ul>

        <input type='hidden' name='survey_id' value='<%= @survey.id%>'>
        <button type='submit' value='submit'>
            Send
        </button>
    </form> <!-- Fin del formulario -->
  </body>
</html>